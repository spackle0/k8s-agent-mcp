apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-delay-example
  namespace: default
spec:
  action: delay
  mode: all
  selector:
    labelSelectors:
      app: example-web
  delay:
    latency: "200ms"
    jitter: "50ms"
  duration: "60s"