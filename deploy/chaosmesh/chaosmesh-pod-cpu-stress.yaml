apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress-example
  namespace: default
spec:
  mode: one
  selector:
    labelSelectors:
      app: example-web
  stressors:
    cpu:
      workers: 2
      load: 80
  duration: "30s"