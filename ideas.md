# Ideas and Todos


* API
  * /prompt - send an arbitrary prompt
  * /check/<name> - run a pre-canned prompt
* Dashboard
* JSON schema checking
* have the agent check for the mcp and the ollama and wait for them to start
* have the server check for its access
  * pick different config files
* logging
* expose metrics to prometheus via /metrics
* add a health check endpoint